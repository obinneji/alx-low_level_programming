#include "main.h"
#include <string.h>
/**
* _strcmp - compares two string
* @s1: the first string
* @s2: the secord string
* Return: an interger
*/

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}

